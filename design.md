项目名称: 排序可视化

逻辑设计:
1.由工具层生成随机数组,通信发往前端层完成柱子初步绘画
2.开始排序,每当后端层排好一个,就把当前数组状态发给前端层更新动画
3,排序结束后给出统计时间

通信设计:
接口约定（排序可视化项目）
1. 排序模块→绘图模块传递的数据：
   - 当前数组：列表
   - 状态数组：列表，
   - 性能数据：字典
2. 状态码→颜色：
   - 0→lightskyblue（待排序）
   - 1→orange（比较中）
   - 2→red（当前操作）
   - 3→forestgreen（已排序）
3. 工具模块生成的随机数组：
   - 格式：列表，无负数、无空值.
   
项目结构：
sort_visualization/  # 项目根目录
├── src/             # 源代码目录（核心功能）
│   ├── __init__.py  # 标记为Python包（空文件即可）
│   ├── sort_algorithms.py  # 后端：排序算法（纯数据处理）
│   ├── utility.py     # 工具层：数据生成/颜色映射
│   ├── visualizer.py # 前端：绘图/动画
│   └── main.py      # 入口层：集成所有模块
└── tests/           # 测试目录
    ├── __init__.py  # 空文件
    ├── test_sort_algos.py  # 测试：排序算法
    |--test_visualizer.py  # 测试：可视化

